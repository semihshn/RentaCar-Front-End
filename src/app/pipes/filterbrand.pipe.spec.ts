import { FilterbrandPipe } from './filterbrand.pipe';

describe('FilterbrandPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbrandPipe();
    expect(pipe).toBeTruthy();
  });
});
