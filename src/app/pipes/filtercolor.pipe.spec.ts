import { FiltercolorPipe } from './filtercolor.pipe';

describe('FiltercolorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercolorPipe();
    expect(pipe).toBeTruthy();
  });
});
